'use strict'

const Server = require('./Server')

async function run () {
    try {    
    } catch (err) {
        console.error('Sorry error happened on starting the application ... ')
        console.error(err)
    }
}

run()